{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "cloud-workspace-projection",
  "description": "Allow the canonical CXApp workspace to persist only safe tenant labels",
  "windows": ["main"],
  "remote": {
    "urls": ["https://app.codexsun.com/*"]
  },
  "permissions": ["core:default"]
}
